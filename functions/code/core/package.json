{
  "name": "backend",
  "private": true,
  "dependencies": {
    "@sendgrid/mail": "^7.7.0",
    "aws-sdk": "^2.904.0",
    "dotenv": "^16.0.3",
    "esbuild": "^0.16.12",
    "ts-node": "^10.9.1"
  },
  "scripts": {
    "build": "tsc",
    "generate": "prisma generate",
    "migrate": "prisma migrate deploy",
    "start:handler": "ts-node handler.ts"
  },
  "devDependencies": {
    "@types/node": "^14.14.35",
    "typescript": "^4.2.3"
  }
}
